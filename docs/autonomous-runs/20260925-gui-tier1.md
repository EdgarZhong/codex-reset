# 自主实现 UAT 记录：GUI Tier 1 转向（2026-09-25 会话三）

> 对应 commit（会话三）：见 `git log`。验收人：主会话（自主执行周期）。被验收版本：已安装 `/Applications/CodexReset.app`（ad-hoc 签名）。

## 用户行为路径 × 验收结果

| # | 用户路径 | 预期 | 验收方法 | 结果 |
|---|---------|------|---------|------|
| P1 | 启动 App，看菜单栏 | 显示用量环 + 恢复倒计时 | kimi-cu 读菜单栏 AX | ✅「⏳ 46分钟」（18:25 → 19:11 正确） |
| P2 | 打开面板概览 | 上限提示、5h/1w 用量、暂停对话列表、自动继续开关、指令框、无 remote_control 设置项 | kimi-cu 截图 | ✅（设置页 remote_control toggle 已移除；辅助功能未授权提示符合 ad-hoc 重签预期） |
| P3 | 勾选对话 → 恢复后自动继续 | GUI Tier 1 事务日志 + 只有 DB 回执确认才「已继续」 | 代码路径 + mock 自测 e01-e05b | ✅（限速中未真实触发；19:11 后真实观察） |
| P4 | 手动「立即继续」 | 无额度门，GUI → Tier 2，失败明确通知 | 代码路径（默认无勾选不发送） | ✅（本轮口径确认无门） |
| P5 | `--query` / `--continue` | 无头正常，跳过 GUI | app 内实跑 | ✅ mode=own-server、13 暂停对话 |
| P6 | GUI Tier 1 事务（限速期） | 粘贴成功也不宣布成功；DB 无落盘 → uncertain / delivered:false | 临时 thread 真实 E2E ×2 | ✅ E2E#1 uncertain + 零落盘；E2E#2 残留跳过粘贴 → delivered:false |
| P7 | GUI Tier 1 事务（恢复后） | DB 回执确认新 turn inProgress → confirmedSuccess | 19:26 一次性任务实跑 `--e2e`（额度 19:11 恢复后） | ✅ `RESULT confirmedSuccess turnId=01a0d84d-…7386`；sqlite 独立验证：新 userMessage ordinal 30（「继续」）、新 turn inProgress；粘贴/聚焦一次通过 |
| P8 | Tier 2 fallback 边界 | delivered:true/uncertain 绝不 Tier 2；仅未提交才 Tier 2 | mock 自测 e02/e03/e04 + 55/55 | ✅ |
| P9 | 限速残留不污染 | composer 已有相同内容跳过粘贴；其它内容不覆盖 | E2E#2 + 代码 review | ✅ |
| P10 | 剪贴板 | 发送后恢复原内容 | 代码（快照+defer 恢复）；E2E 期间无异常 | ✅ |

## 证据索引

- mock 自测 55/55：`/tmp/codex-reset-selftest/`（main.swift + fake_codex.py + db_fixture.py，易失；编译命令见 CLAUDE.md §5）
- E2E#1/#2 输出：本会话后台任务日志（`--e2e 01a0d813-… "继续"`）
- Desktop 限速置灰截图：用户提供的面板截图（composer 含「继续」+ 发送按钮灰）
- AX 取证：axdump 输出（Electron 树、AXTextArea [422,732 713x44] value=继续）

## 遗留与下一轮输入

1. ~~P7 恢复后 confirmed 路径~~（19:26 已实证：confirmedSuccess，见 P7 行）
2. 19:12 真实 auto-continue 观察：GUI Tier 1 聚焦成功但粘贴被用户切窗打断（→ 已实施粘贴 3 轮重试，含重激活+重聚焦）；Tier 2 兜底遇 `already has an active writer`（thread 在 GUI 中打开、被 GUI 内核持有，属预期）
3. GUI 无权限路径未实测（本机 Bash 子进程恒有 AX 权限）
4. Desktop 冷启动分支未实测
5. kimi-cu 提示可升级（0.5.10 → 0.6.1），与本体无关

## 推荐用户抽查路径

恢复后（19:11 之后）：面板勾选 1 个不重要的暂停对话 → 点「立即继续」→ 观察 ChatGPT Desktop 自动跳到该对话并发出「继续」→ 面板日志出现「GUI Tier 1 回执确认：prompt 已进入 thread 并启动 turn …」→ 对话在 GUI 中可见地继续。
